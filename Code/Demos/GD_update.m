function[theta_new] = GD_update(theta)

theta_new = zeros(3,1);

for ind = 1:length(theta_new)
    switch ind
        case 1
            theta_new = theta;
        case 2
            theta_new = theta;
        case 3
            theta_new = theta;
        otherwise
            disp('error')
    end
                
end
        
    theta_new(i) = 
end

    
    